#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@dillonkearns/elm-graphql/bin/elm-graphql" "$@"
  ret=$?
else 
  node  "$basedir/../@dillonkearns/elm-graphql/bin/elm-graphql" "$@"
  ret=$?
fi
exit $ret
